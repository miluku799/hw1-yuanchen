# hw1-yuanchen

# Homework 1: Coding Assistant Onboarding

**Repository Description:**
This repository contains my submission for Homework 1, demonstrating a basic workflow using an AI coding assistant, manual file editing, and version control using Git and GitHub.

**Git Steps Used:**
I used the command line to initialize my repository (`git init`), add files (`git add`), and save snapshots of my progress (`git commit -m "..."`). Finally, I pushed the local repository to GitHub using `git push`.

**Commit History Summary:**
* `Add my own edited version`
* `Add assistant-created draft`
* `Initial repository setup`
* `Updating the README`
